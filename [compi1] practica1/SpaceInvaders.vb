Public Class SpaceInvaders

    Private Sub SpaceInvaders_Load(sender As Object, e As EventArgs) Handles MyBase.Load
        'Dim datos As String = Juego.obtener_eleccion()
        'cargar_contenido(datos)
    End Sub

    Private Sub cargar_contenido(datos As String)
        'Dim datos_split() As String= datos.Split(";")
        'fondo;sonido;id
        'Me.BackgroundImage = Image.FromFile(datos_split(0))
    End Sub

    Private Sub cargar_tablero()

    End Sub
End Class